{
  "name": "nexusserve-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create nexusserve-db",
    "d1:migrate": "wrangler d1 migrations apply nexusserve-db --local",
    "d1:migrate:prod": "wrangler d1 migrations apply nexusserve-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0"
  }
}
